# UI 设计规范文档 v2.0 — 数据分析类产品

## 文档信息

- 版本：v2.0
- 更新日期：2025-12-29
- 适用范围：数据分析类产品（看板、穿透页、弹窗）
- 协作方：需求人员、UI 设计师、前端开发、配置开发

---

## 一、整体说明

### 1.1 设计协同流程

需求与UI设计 → 实现与交付

- 业务与客户确认需求（业务逻辑与展示数据）
- 需求人员使用模版绘制原型（PPT模版）
- UI人员提供界面设计模版（PPT或MG）
- 需求人员选定布局与界面模版
- 配置或开发人员使用工具实现模版
- 演示验收与上线

### 1.2 标识规范

1. 看板首页需要有系统头部；穿透页不需要有系统头部。
2. 页面名称必有，可根据业务选配内容。

### 1.3 文本与数据规范

- 整体字体使用：系统默认，根据设计规范分别使用"不加粗"和"加粗"两种。
- 数值单位换算规则：
  - 整位数达到5位需换算为万
  - 整位数达到9位需换算为亿
  - 所有数值都应有千分位符号
  - 所有金额都应有小数点两位
- 示例：
  - 数值：1,000人 / 1万人 / 1,000万人 / 1亿人
  - 金额：1,000.00元 / 1.00万元 / 1,000.00万元 / 1.00亿元

### 1.4 主页设计参考

**布局选择逻辑**：

1. 第一步：选择主页类型（数据指标主页 / 业务关系主页 / 业务流程主页）
2. 第二步：根据需展示的数据量选择布局方案

**数据指标主页**：

- 数据区块较多时选择中心布局或并列布局
- 有一项核心数据区块或有地图时选择中心布局
- 仅有一主一辅两组信息或有指标条时选用上下布局

**业务关系主页**：

- 仅需展示业务关系网络图时选择单块布局
- 需指标条强化分析时选择上下布局
- 需要展示大量辅助指标数据时选择中心布局

**业务流程主页**：

- 仅需展示业务流程图时选择单块布局
- 需指标条强化分析时选择上下布局
- 需要展示大量辅助指标数据时选择中心布局

### 1.5 功能页整体布局

**内容区**：页面信息内容展示区域

**区块卡片**：划分和承载不同主题信息

- 由"区块标题"及"数据内容"组成
- 特定场景还需要有"功能操作区"及"选项切换"（非必选）

**栅格系统**：

- 采用24栅格
- 左右边距32px
- 水槽24px
- 内容区上下边距24px
- 上下卡片间距24px
- 区块卡片需横向完整覆盖N条栅格，建议N为偶数

**排版布局要求**：

- 避免信息过载，尽量在第一屏展示完整的核心指标
- 总模块数量控制在5-9个
- 若页面信息较多无法在一屏展示采用瀑布流式布局

**布局选择原则**：

- 上下布局：将信息按"主-次"上下结构排列
- 中心布局：围绕某一主题而展开
- 并列布局：当指标重要性平均时，整体平铺布局
- 左右布局：并列布局的一种，页面信息区块较少时采用
- 存在指标条时默认选用上下布局
- 有地图板块时默认选用中心布局

### 1.6 指标条规范

**指标条定义**：横长条，通常位于上下布局的上方，用于展示该场景下的重要指标。

**指标卡布局**：

- 横向指标卡：单行2、3、4项指标时选择
- 竖向指标卡：单行5、6、6+项指标时选择
- 单行5项指标时若下方内容较多空间有限可选择横向指标

**指标卡ICON**：

- 非必选，ICON主要为装饰作用
- 根据指标卡片内容饱满程度选择，若内容饱满不添加
- 单行6+项指标时不添加ICON

**指标卡内容类型**：

- 单指标：3种样式布局，纵向空间充足时选择1>3>2
- 多指标：次指标少于4项时选用单列，多于4项时选用双列，次指标不超过8项
- 双列多指标不可用于指标卡多列排布中

**指标单行个数**：

- 常见情况有2-6项指标
- 单行指标项建议不超出6项
- 超过6项时使用纯文字的单指标
- 单行展示不下时出现左右切换按钮
- 内容区支持多列排布，竖向指标不得超过两列

**指标卡宽度**：

- 同一指标条上的指标卡宽度需相同
- 不得小于144px

**自定义指标卡**：

- 支持内嵌图表等自定义样式

**指标卡文字**：

- 文字溢出时省略显示"…"，鼠标悬停时显示完整字段
- 数字类数据不可省略

**数字格式规则**：

- 达到万级自动换算成万，达到亿级换算成亿
- 保留小数点后两位，四舍五入
- 所有数字数据均需使用千分位
- 数字金额最小单位为元，保留小数点后两位

**指标卡混搭说明**：

- 同一指标条内指标卡建议采用相同样式
- 横向指标不能与竖向指标卡混搭
- 自定义指标卡可与横向指标及竖向指标卡其一混搭

---

## 二、配置工具已有模版

### 2.1 看板类概览

**布局结构**：上（左中右）板块 + 下（上下）板块

**上半部分**：

- 左右为数据指标展示，中间为地图或热力图
- 左右数据指标数量：单页最少3个，最多5个

**下半部分**：

- 业务关系的图表、数据概览、分类、步骤详情展示
- 面板最小高度234px，最大高度500px
- 带图标的指标：一行最少1个，最多4个均分，仅支持一行
- 带项目符号的指标：一行最少1个，最多2个均分，可支持多行

### 2.2 弹窗类概览

**弹窗尺寸**：

- 大弹窗：1000px × 610px
- 中弹窗：800px × 500px
- 小弹窗：400px × 250px

**按钮规范**：

- 底部有操作按钮时，必须有一个焦点按钮（绿色）
- 焦点按钮只能在最右侧

### 2.3 穿透页面概览

**布局结构**（从上到下）：

1. 标题区：文字为绿色，背景高度固定，背景投影，默认展开
2. 查询条件区：查询面板有投影
3. 查询结果区：表格背景白色面板左右通栏

**指标区样式**：

- 样式一：带指标组的布局
- 样式二：相同样式的指标，独立面板布局

---

## 三、布局模版

### 3.1 基础布局类型

每种布局均提供"有指标栏"和"无指标栏"两种变体。

| 序号 | 布局名称 | 说明 |
|------|---------|------|
| 1 | 单区块布局 | 单一内容区块，适用于核心图表展示 |
| 2 | 上下布局 | 信息按"主-次"上下结构排列 |
| 3 | 左右布局 | 两列并排，适用于对比分析 |
| 4 | 三列等分布局 | 三列均分，适用于多维度展示 |
| 5 | 2×2四等分布局 | 四宫格，适用于综合概览 |
| 6 | 2×2七三占比布局 | 左侧70%右侧30%，适用于主辅搭配 |
| 7 | 2×2三七占比布局 | 左侧30%右侧70%，适用于辅主搭配 |
| 8 | 2×3六等分布局 | 两行三列，适用于多指标监控 |
| 9 | 3×2六等分布局 | 三行两列，适用于分类展示 |
| 10 | 上一下二布局 | 上方大图+下方两个小图 |
| 11 | 上一下三布局 | 上方大图+下方三个小图 |
| 12 | 上二下一布局 | 上方两个+下方大图 |
| 13 | 上二下三布局 | 上方两个+下方三个 |
| 14 | 上三下一布局 | 上方三个+下方大图 |
| 15 | 上三下二布局 | 上方三个+下方两个 |
| 16 | 左一右二布局 | 左侧大图+右侧两个小图 |
| 17 | 左一右二布局（七三占比） | 左侧70%+右侧30%两个 |
| 18 | 左二右一布局 | 左侧两个+右侧大图 |
| 19 | 左二右一布局（三七占比） | 左侧30%两个+右侧70% |
| 20 | 半包型中心布局 | 中心焦点+周围辅助 |
| 21 | 全包型中心布局 | 中心大图+四周环绕 |
| 22 | 左上型布局 | 左上大区域+右下辅助 |

---

## 四、原型模版

### 4.1 页面结构

完整页面包含以下层级：

1. **系统头部**：Logo + 导航菜单 + 用户信息
2. **页面标题栏**：页面标题 + 单位选择 + 报告期 + 全局按钮
3. **指标条区域**：关键指标展示（可选）
4. **内容区域**：图表、表格、数据展示

### 4.2 导航菜单

- 一级菜单 + 二级菜单 + 末级菜单
- 典型菜单项：应用总览、共享财务、业务财务、战略财务、工作台、问题反馈

---

## 五、原型元素

### 5.1 指标卡组件

**类型A：无图标指标卡**

- 贯穿式卡片：指标卡横向排列，占据整行
- 独立式卡片：每个指标卡独立成块

**类型B：有图标指标卡**

- ICON为非必选装饰元素
- 单指标ICON不与指标卡ICON同时存在

**指标卡尺寸规范**：

| 尺寸 | 适用场景 |
|------|---------|
| 8px间距 | 紧凑型指标 |
| 12px间距 | 标准型指标 |
| 16px间距 | 舒适型指标 |
| 20px间距 | 宽松型指标 |
| 24px间距 | 大尺寸指标 |
| 28px间距 | 超大尺寸指标 |
| 32px间距 | 特大尺寸指标 |
| 48px间距 | 强调型指标 |

**指标卡内容结构**：

- 主指标名称 + 数值 + 单位 + 占比
- 支持单指标和多指标布局
- 多指标单列（<4项次指标）
- 多指标双列（4-8项次指标）

### 5.2 图表组件

**支持的图表类型**：

| 图表类型 | 用途 | 说明 |
|---------|------|------|
| 饼图 | 占比分析 | 支持环形图变体 |
| 柱状图 | 对比分析 | 支持渐变色、圆角 |
| 条形图 | 排名分析 | 水平方向柱状图 |
| 折线图 | 趋势分析 | 支持面积填充 |
| 面积图 | 累积趋势 | 支持堆叠面积 |
| 组合图 | 多维分析 | 柱状+折线组合 |
| 地图 | 区域分析 | 支持热力图、着色地图 |
| 雷达图 | 多维评估 | 多维度对比 |
| 仪表盘 | 进度展示 | 目标完成度 |
| 漏斗图 | 转化分析 | 流程转化率 |

**图表标题规范**：

- 图表标题位于图表上方
- 标题文字使用加粗样式
- 图表单位标注在标题旁或图表右下角

### 5.3 图标规范

**图标状态**：

- 默认状态：灰色描边
- 选中状态：白色描边 + 绿色背景填充

**图标尺寸**：

- 标准尺寸：24×24px
- 大尺寸：32×32px

**图标风格**：

- 线性图标（outlined）
- 描边宽度：2px
- 圆角端点和连接

### 5.4 表格组件

**表格结构**：

- 表头：浅灰色背景，加粗文字
- 数据行：白色背景，水平分隔线
- 分页：每页20条，支持跳转

**表格交互**：

- 分页控件：上一页、下一页、页码跳转
- 每页条数：默认20条/页
- 排序：支持列头点击排序

**表格规范**：

- 表格背景白色面板左右通栏
- 共X条数据提示
- 页码导航：1 2 3 4 5 ... 7 前往 页

### 5.5 按钮组件

**按钮类型**：

| 类型 | 样式 | 用途 |
|------|------|------|
| 主按钮 | 绿色背景 + 白色文字 | 主要操作 |
| 次按钮 | 白色背景 + 绿色边框 | 次要操作 |
| 文字按钮 | 无背景 + 绿色文字 | 辅助操作 |

**弹窗按钮规范**：

- 底部操作按钮必须有一个焦点按钮（绿色）
- 焦点按钮只能在最右侧

### 5.6 筛选组件

**筛选区域**：

- 筛选标签 + 下拉选择框
- 支持按维度切换
- 支持关键字搜索输入

---

## 六、颜色规范

### 6.1 主色

| 颜色名称 | 色值 | 用途 |
|---------|------|------|
| 品牌绿 | `#00A896` | 主操作、选中态、品牌标识 |
| 深绿 | `#0D7377` | 深色背景、标题栏 |
| 浅绿 | `#E8F6F3` | 浅色背景、悬停态 |

### 6.2 功能色

| 颜色名称 | 色值 | 用途 |
|---------|------|------|
| 成功绿 | `#52C41A` | 正向指标、成功状态 |
| 警告橙 | `#FA8C16` | 警告提示、关注数据 |
| 错误红 | `#FF4D4F` | 错误状态、负向指标 |
| 信息蓝 | `#1890FF` | 信息提示、链接 |

### 6.3 中性色

| 颜色名称 | 色值 | 用途 |
|---------|------|------|
| 标题文字 | `#1F1F1F` | 一级标题 |
| 正文文字 | `#333333` | 正文内容 |
| 次要文字 | `#666666` | 辅助说明 |
| 占位文字 | `#999999` | placeholder |
| 禁用文字 | `#BFBFBF` | 禁用状态 |
| 边框 | `#E8E8E8` | 分割线、边框 |
| 页面背景 | `#F5F7FA` | 页面底色 |
| 卡片背景 | `#FFFFFF` | 内容卡片 |
| 表头背景 | `#FAFAFA` | 表格表头 |

### 6.4 图表配色

| 序号 | 色值 | 用途 |
|------|------|------|
| 图表色1 | `#2E7AD2` | 主数据系列 |
| 图表色2 | `#70AD47` | 第二数据系列 |
| 图表色3 | `#ED7D31` | 第三数据系列 |
| 图表色4 | `#C5504B` | 第四数据系列 |
| 图表色5 | `#5B9BD5` | 第五数据系列 |
| 图表色6 | `#FFC000` | 第六数据系列 |

---

## 七、字体规范

### 7.1 字体族

```
font-family: system-ui, -apple-system, "PingFang SC", "Microsoft YaHei", "Helvetica Neue", sans-serif;
```

### 7.2 字号层级

| 层级 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| H1 | 20px | Bold (700) | 28px | 页面标题 |
| H2 | 16px | Bold (700) | 24px | 区块标题 |
| H3 | 14px | Medium (500) | 22px | 图表标题 |
| Body | 14px | Regular (400) | 22px | 正文内容 |
| Caption | 12px | Regular (400) | 20px | 辅助说明 |
| Small | 12px | Regular (400) | 18px | 表格内容 |

### 7.3 数字字体

```
font-variant-numeric: tabular-nums;
```

所有数字数据使用等宽数字，确保对齐。

---

## 八、间距规范

### 8.1 间距系统

基于 8px 栅格系统：

| Token | 值 | 用途 |
|-------|-----|------|
| space-xs | 4px | 图标间距、紧凑内边距 |
| space-sm | 8px | 组件内边距、紧凑间距 |
| space-md | 12px | 标准间距 |
| space-base | 16px | 卡片内边距、组件间距 |
| space-lg | 24px | 区块间距、卡片间距 |
| space-xl | 32px | 页面边距 |
| space-xxl | 48px | 大间距 |

### 8.2 栅格系统

- 栅格数：24
- 左右边距：32px
- 水槽（gutter）：24px
- 内容区上下边距：24px
- 上下卡片间距：24px

---

## 九、圆角与阴影

### 9.1 圆角

| 元素 | 圆角值 |
|------|--------|
| 按钮 | 4px |
| 卡片 | 4px |
| 输入框 | 4px |
| 弹窗 | 8px |
| 标签/徽章 | 12px |
| 图标背景 | 50%（圆形） |

### 9.2 阴影

| 层级 | 值 | 用途 |
|------|-----|------|
| shadow-sm | `0 1px 2px rgba(0,0,0,0.03)` | 轻微浮起 |
| shadow-md | `0 2px 8px rgba(0,0,0,0.08)` | 卡片浮起 |
| shadow-lg | `0 4px 16px rgba(0,0,0,0.12)` | 弹窗浮起 |

---

## 十、响应式断点

| 断点名称 | 宽度 | 适用场景 |
|---------|------|---------|
| xl | ≥1920px | 大屏显示器 |
| lg | ≥1440px | 标准桌面 |
| md | ≥1280px | 小屏桌面/笔记本 |
| sm | ≥768px | 平板 |

> 本系统为桌面端优先设计，主要适配 1440px 及以上分辨率。

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| 指标条 | 横向排列的指标卡集合 |
| 指标卡 | 展示单个指标数据的卡片组件 |
| 区块卡片 | 划分和承载不同主题信息的容器 |
| 栅格 | 用于划分区块卡片的网格系统 |
| 穿透页 | 从看板点击数据后跳转的明细页面 |
| 看板 | 数据分析的主页面，展示关键指标和图表 |
| 水槽 | 栅格之间的间距 |

### B. 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2025-11-03 | 初始版本 |
| v2.0 | 2025-12-29 | 新增原型元素规范、完善布局模版、补充颜色和字体规范 |
